spring:
  application:
    name: pms-backend

  profiles:
    active: dev

server:
  port: ${SERVER_PORT:8080}

app:
  jwt:
    secret: ${JWT_SECRET:change-this-secret-to-a-very-long-production-secret-key-1234567890}
    expiration-minutes: ${JWT_EXPIRATION_MINUTES:480}